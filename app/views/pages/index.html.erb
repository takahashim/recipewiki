<h1>ページ一覧</h1>

<div class="hatena-body">
<div class="main">

<div id="days">
<div class="day">
<div class="body">
<div class="section">

<table>

<% @pages.each do |page| %>
  <tr>
    <th><%=link_to h(page.title), show_page_path(page.path) %></th>
    <td>(<%=h page.updated_at %> 更新)</td>
  </tr>
<% end %>
</table>

</div>

<!--
<%= link_to 'New page', new_page_path %>
-->
</div>
</div>
</div>

<div class="calendar" id="pager-bottom">
</div>
</div>
